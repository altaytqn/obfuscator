#ifndef CPP_CORE_HPP
#define CPP_CORE_HPP

#include <string>
#include <vector>

namespace Core {
    std::string randomInsert(const std::string& input, const std::vector<char>& symbols);
}

#endif
